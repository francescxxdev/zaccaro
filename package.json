{
  "name": "zaccaros-world-cup",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "seed": "node scripts/seed.js",
    "dev": "concurrently \"PORT=3080 node server.js\" \"npm run dev --prefix client\"",
    "build": "npm install --prefix client --include=dev && npm run build --prefix client"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.7.0",
    "concurrently": "^9.1.2",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2"
  }
}
